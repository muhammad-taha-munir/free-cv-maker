# TODO - Fix PDF export error (oklab)

- [ ] Implement runtime normalization of CSS colors (strip/avoid oklab/oklch) before html2canvas capture in `src/App.jsx`.
- [ ] Restore original inline styles after capture to avoid UI side-effects.
- [ ] Run `npm test`/lint if available or `npm run build` and `npm run dev` to verify PDF export works.

